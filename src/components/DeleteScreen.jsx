import { Delete24Regular } from "@fluentui/react-icons";

const DeleteScreen = (props) => {
  return (
    <div {...props}>
      <Delete24Regular className="delete-icon" />
    </div>
  );
};

export default DeleteScreen;
